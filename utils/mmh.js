'use strict';

var models  = require('../../models');

addTestVenues();

function addTestVenues(num){
  if(!num){
    num = 20;
  }

  for(var i=0;i < num; i++){

  }
}

function createTestAddresses(){
  //return new Promise()
}
